<?xml version="1.0" encoding="utf-8"?>
<!--
    dimens.xml — All size / spacing values
    🎨 Edit here to adjust layout without touching XML layouts.
-->
<resources>
    <!-- ── Lobby ──────────────────────────────────────────────────────── -->
    <dimen name="lobby_title_size">36sp</dimen>
    <dimen name="lobby_title_margin_bottom">48dp</dimen>
    <dimen name="lobby_button_width">220dp</dimen>
    <dimen name="lobby_button_text_size">18sp</dimen>
    <dimen name="lobby_button_gap">16dp</dimen>

    <!-- ── HUD ───────────────────────────────────────────────────────── -->
    <dimen name="hud_text_size">16sp</dimen>
    <!-- Right margin keeps the flip counter from overlapping the pause icon -->
    <dimen name="hud_flip_margin_end">52dp</dimen>

    <!-- ── Card grid ──────────────────────────────────────────────────── -->
    <dimen name="grid_padding">8dp</dimen>
    <dimen name="card_size">76dp</dimen>
    <dimen name="card_margin">4dp</dimen>
    <dimen name="card_corner_radius">10dp</dimen>
    <dimen name="card_elevation">4dp</dimen>
    <dimen name="card_front_padding">8dp</dimen>

    <!-- ── Result dialog ──────────────────────────────────────────────── -->
    <dimen name="dialog_padding">24dp</dimen>
    <dimen name="result_title_size">24sp</dimen>
    <dimen name="result_stat_size">17sp</dimen>
    <dimen name="result_inner_gap">16dp</dimen>
    <dimen name="result_stat_gap">12dp</dimen>
    <dimen name="result_button_margin_top">16dp</dimen>
    <dimen name="result_button_gap">8dp</dimen>
</resources>
