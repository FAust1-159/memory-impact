<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_game.xml — Game screen

    Layout structure:
      [Toolbar]            ← time (left) | pause icon (right)
      [Flip counter row]   ← flips label below toolbar
      [RecyclerView]       ← 4×4 card grid (fills remaining space)

    🎨 CUSTOMISE:
      • Background  → change android:background to @drawable/bg_game
      • HUD font    → android:fontFamily on tvTimer / tvFlipCount
      • Colors      → colors.xml
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_game">

    <!-- ── AppBar / Toolbar ───────────────────────────────────────────────── -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/hud_bar_bg"
        app:elevation="0dp">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:contentInsetStart="0dp"
            app:contentInsetStartWithNavigation="0dp">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <!-- Timer — left -->
                <TextView
                    android:id="@+id/tvTimer"
                    style="@style/TextAppearance.MemoryMatch.Hud"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:text="@string/hud_time_default"
                    android:textSize="@dimen/hud_text_size"
                    android:textColor="@color/hud_text_color" />

                <!-- Score — center-ish (between timer and flips) -->
                <TextView
                    android:id="@+id/tvScore"
                    style="@style/TextAppearance.MemoryMatch.Hud"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:layout_constraintStart_toEndOf="@id/tvTimer"
                    app:layout_constraintEnd_toStartOf="@id/tvFlipCount"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:text="Score: 0"
                    android:textSize="@dimen/hud_text_size"
                    android:textColor="@color/hud_text_color" />

                <!-- Flips — right -->
                <TextView
                    android:id="@+id/tvFlipCount"
                    style="@style/TextAppearance.MemoryMatch.Hud"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="@dimen/hud_flip_margin_end"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent"
                    android:text="@string/hud_flips_default"
                    android:textSize="@dimen/hud_text_size"
                    android:textColor="@color/hud_text_color" />

            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.appcompat.widget.Toolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- ── Card grid ─────────────────────────────────────────────────────── -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCardGrid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:padding="@dimen/grid_padding"
        android:clipToPadding="false"
        android:overScrollMode="never" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
